# TUTORIAL-REVIEW — インタラクティブチュートリアル設計レビュー

> **目的**: 武器取得時のステップバイステップチュートリアルの設計を議論・確定する  
> **形式**: 各項目について議論→決定の順で記録  
> **関連**: STAGE-SPEC.md（ステージ設計）、UI-SPEC.md（UI仕様）、ENGINE-SPEC.md（エンジン仕様）

---

## 背景

**現行設計**:
- 武器取得画面（Weapon Get）: 新コマンドの名前とキーを表示するだけ
- Teachステージ: 新コマンド1回で解けるステージ（変更1箇所、余裕多め）
- ヒント: ☆1コストでオーバーレイデモ

**提案（ユーザー要望）**:
- 武器取得タイミングで**スキップ可能なステップバイステップチュートリアル**を追加
- ナビゲーターが「次は`l`を押してみよう！」とつぶやく
- 指示されたキー以外は受け付けない（強制誘導）
- わざと間違えさせてundoを教える等の教育的演出
- 既存のTeachステージは変更なし（チュートリアル後にTeachに進む）

---

## 議題一覧

| ID | 項目 | 状態 |
|----|------|------|
| T1 | チュートリアルの発動タイミングと画面フロー | **確定** |
| T2 | ステップの設計構造（データモデル） | **確定** |
| T3 | ナビゲーターUIの設計 | **確定** |
| T4 | 入力制限と誤入力の扱い | **確定** |
| T5 | undo/意図的失敗の教育設計 | **確定** |
| T6 | スキップ機能の設計 | **確定** |
| T7 | どのノードにチュートリアルを付けるか | **確定** |
| T8 | チュートリアルとTeachステージの関係 | **確定** |
| T9 | チュートリアルのデータ形式と管理 | **確定** |
| T10 | 進行状態の永続化 | **確定** |

---

## T1: チュートリアルの発動タイミングと画面フロー

**決定**: 案A — Weapon Get直後

```
ノードクリア → [Weapon Get画面] → [チュートリアル] → ツリー画面 → [Teachステージ]
```

- 武器取得の直後に「試し振り」として発動
- チュートリアル完了（またはスキップ）後にツリー画面へ戻る
- Teachステージは変更なし（チュートリアル = 練習場、Teach = 初の実戦）

---

## T2: ステップの設計構造（データモデル）

**決定**: 1ステップ = message + expectedKey + acceptedKeys + editorSetup + wrongKeyMessage

| 要素 | 説明 |
|------|------|
| `message` | ナビゲーターのセリフ |
| `expectedKey` | 正解キー（nullなら任意キーで次へ） |
| `acceptedKeys` | 受け付ける全キー（省略時=expectedKeyのみ） |
| `editorSetup` | ステップ開始時のエディタ状態（省略時=前ステップ引き継ぎ） |
| `wrongKeyMessage` | 誤入力時ナビメッセージ（省略時=デフォルト） |

- 意図的失敗ステップ（undo教育等）もこの構造で表現可能
- 自由入力ステップ: `acceptedKeys`に複数キー + `expectedKey`に終了キー

---

## T3: ナビゲーターUIの設計

**決定**:
- キャラクター付き（最小限のアイコン+名前+セリフ）
- 表示位置: 画面下部固定バー（RPG会話ウィンドウ風）
- チュートリアル中はHAND（手札パネル）を非表示にして、そのスペースにナビを表示

---

## T4: 入力制限と誤入力の扱い

**決定**:
- 誤入力: キーは無視 + ナビが反応（期待キーを再表示）
- メッセージ: 固定（バリエーションはv2.0で検討）

---

## T5: undo/意図的失敗の教育設計

**決定**:
- undoチュートリアル: N02（x初習得時）に組み込む
- 流れ: x実行→消しすぎ→ナビ「uを押せ」→undo体験→「いつでも使える安全網」説明
- T2のステップ構造で表現可能（特別な仕組み不要）

---

## T6: スキップ機能の設計

**決定**:
- 粒度: チュートリアル全体スキップ（ステップ単位不可）
- 操作: Esc + 画面隅「Skip」ボタン（両方）

---

## T7: どのノードにチュートリアルを付けるか

**決定**: 新概念導入ノードのみ（7-8ノード）

| ノード | 新概念 |
|--------|--------|
| N01 | 基本移動（hjkl） |
| N02 | 初の編集（x）+ undo導入 |
| N03 | Insertモード（i/a） |
| N08 | 単語移動（w/b/e） |
| N18 | オペレータ+モーション（dw） |
| N29 | TextObj（iw/aw） |
| N37 | Visualモード（v/V） |

残り33ノードは武器取得画面の説明 + Teachステージで対応。

---

## T8: チュートリアルとTeachステージの関係

**決定**: 両方残す（2段階）
- チュートリアル = ガイド付き練習（指示通り押すだけ）
- Teach = 初の自力プレイ（ガイドなし、簡単）
- 冗長に感じる人はチュートリアルをスキップ

---

## T9: チュートリアルのデータ形式と管理

**決定**:
- 専用ディレクトリ: `data/tutorials/N01.ts` 等
- ステージデータとは分離（型が異なるため）
- TutorialStep型で定義

```typescript
interface TutorialStep {
  message: string;
  expectedKey: string | null;
  acceptedKeys?: string[];
  editorSetup?: { text: string; cursor: { line: number; col: number } };
  wrongKeyMessage?: string;
}
```

---

## T10: 進行状態の永続化

**決定**:
- 完了/スキップのみ保存（途中状態は不要）
- GameProgressに `tutorialStatus: Record<string, 'completed' | 'skipped'>` を追加

---

## 議論ログ

| ID | 議論日 | 決定内容 | 備考 |
|----|-------|---------|------|
| T1 | 2026-05-23 | Weapon Get直後に発動。チュートリアル→ツリー→Teach | 案A |
| T2 | 2026-05-23 | message+expectedKey+acceptedKeys+editorSetup+wrongKeyMessage | 意図的失敗も表現可能 |
| T3 | 2026-05-23 | キャラ付き下部バー。チュートリアル中はHAND非表示 | RPG会話ウィンドウ風 |
| T4 | 2026-05-23 | 誤入力=無視+ナビ反応（期待キー再表示）。固定メッセージ | |
| T5 | 2026-05-23 | N02でundo導入。x消しすぎ→u復元の流れ | T2構造で表現可能 |
| T6 | 2026-05-23 | 全体スキップ。Esc+画面隅Skipボタン | |
| T7 | 2026-05-23 | 新概念導入7ノード（N01,02,03,08,18,29,37） | 残り33ノードはTeachで |
| T8 | 2026-05-23 | チュートリアル+Teach両方残す。「教わる→自分でやる」2段階 | スキップ可で冗長回避 |
| T9 | 2026-05-23 | data/tutorials/専用ディレクトリ。TutorialStep型 | ステージとは分離 |
| T10 | 2026-05-23 | GameProgressにtutorialStatus追加。completed/skippedのみ | 途中保存不要 |
| T11 | 2026-05-23 | 7チュートリアルノードではWeaponのTRY ITを省略、チュートリアルで代替。33ノードはTRY IT維持 | 整合性チェックで発見。案A採用 |

